import MessagesPage from '@/pages/coach/Messages'

// Parent messaging reuses the same Messages component —
// the thread list and real-time subscription are user-ID based,
// so parents see their own threads with coaches automatically.
export default MessagesPage
