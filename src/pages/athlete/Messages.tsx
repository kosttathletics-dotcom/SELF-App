import MessagesPage from '@/pages/coach/Messages'

// Athlete messaging reuses the same Messages component —
// thread list and real-time subscription are scoped to the logged-in user's ID.
export default MessagesPage
